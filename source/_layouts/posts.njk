---
layout: default
---

{%- from "hero.njk" import hero with context -%}
{%- from "post-meta.njk" import postMeta with context -%}

<article>

  {{- hero(
      banner=banner,
      title=title
  ) -}}

  {{- postMeta(
      date=date,
      tags=tags
  ) -}}

  {{ content|safe }}

</article>
